CREATE TABLE user_dest (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  user_id INTEGER REFERENCES t-travels_users(id) ON DELETE CASCADE,
  dest_id INTEGER REFERENCES destinations(id) ON DELETE CASCADE,
  goal_date DATE,
  budget DECIMAL(12,2)
);